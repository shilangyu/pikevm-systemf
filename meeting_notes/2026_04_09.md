## Collected notes

## To discuss

- Instead of playing with indices, embed `input -> bool` directly in the `OracleQuery`? Then, `OracleWrite` would also be taking a function. `OracleWrite` would be a side effect. -> no because the bytecode would be harder to lower

## Action items
